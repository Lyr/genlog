MESSAGE="[${TIMESTAMP}] (${USER_ID}) Tout va bien;0 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Tout va mal;5 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Ceci est un test;6 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) FOO;7 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Oups;8 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Alert, code nine;9 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Armagedon;10 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Devil s number;666 | val=${VALUE}"
MESSAGE="[${TIMESTAMP}] (${USER_ID}) Zen attitude;11 | val=${VALUE}"
